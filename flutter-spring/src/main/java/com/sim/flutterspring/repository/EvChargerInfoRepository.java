package com.sim.flutterspring.repository;

import com.sim.flutterspring.entity.EvChargerInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EvChargerInfoRepository extends JpaRepository<EvChargerInfo,Long> {
}
